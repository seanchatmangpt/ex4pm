# ex4pm v26.8.22 Final Closure Crown

Status: implementation in progress on `release/v26.8.22-final-crown`.

This release preserves the repository governing calculus:

`observation -> parse -> route -> admit/refuse -> construct -> BRCE -> DO -> receipt -> replay -> standing`.

The release crown is downstream qualification. It does not redefine canonical semantics or grant infrastructure authority to ex4pm.

## Closure obligations

1. bounded POWL soundness/completeness with an independent oracle and compiler-refinement evidence;
2. capability-indexed execution-rail qualification and differential normalization;
3. durable ambiguity closure for distributed consequential execution;
4. independently attested topology and artifact/build provenance;
5. independent standing recomputation plus sabotage detection;
6. Hex package construction and exact artifact inspection before publish actuation.

Repository-level `ALIVE` is computed only when every mandatory evidence class is present and verifies. Missing external multi-region evidence remains `PARTIAL_ALIVE`; no manual override exists.
