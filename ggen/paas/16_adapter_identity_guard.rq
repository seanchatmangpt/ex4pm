PREFIX ex4pm: <https://seanchatman.github.io/ex4pm/paas#>
SELECT ?run WHERE {
  ?run a ex4pm:PaaSRun .
  FILTER NOT EXISTS { ?run ex4pm:adapterIdentity ?adapter . FILTER(STRLEN(STR(?adapter)) > 0) }
}
