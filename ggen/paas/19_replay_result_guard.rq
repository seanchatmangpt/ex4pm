PREFIX ex4pm: <https://seanchatman.github.io/ex4pm/paas#>
SELECT ?replay WHERE {
  ?replay a ex4pm:Replay .
  FILTER NOT EXISTS { ?replay ex4pm:resultIdentity ?result . FILTER(STRLEN(STR(?result)) > 0) }
}
