name: explore-semantic-courts
on:
  pull_request:
    paths:
      - 'scripts/explore/semantic/**'
      - '.github/workflows/explore-semantic-courts.yml'
permissions:
  contents: read
jobs:
  semantic-courts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: '1.18.4'
          otp-version: '27.3.4'
      - name: Verify exact subject and execute semantic courts
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          count=0
          for court in scripts/explore/semantic/*.exs; do
            elixir "$court"
            count=$((count+1))
          done
          test "$count" -ge 8
